{
  "name": "holyjs-webvrtoday",
  "version": "0.0.1",
  "description": "Repo with examples, made for HolyJS conference",
  "main": "index.js",
  "author": "Miroshnichenko Anton",
  "license": "ISC",
  "scripts": {
    "start:web:gl": "http-server -o /webgl -S -C ./certs/localhost.pem -K ./certs/localhost-key.pem",
    "start:web:gpu": "http-server -o /webgpu -S -C ./certs/localhost.pem -K ./certs/localhost-key.pem",
    "start:web:xr": "http-server -o /webxr -S -C ./certs/localhost.pem -K ./certs/localhost-key.pem",
    "start:three": "http-server -o /threejs -S -C ./certs/localhost.pem -K ./certs/localhost-key.pem",
    "start:aframe": "http-server -o /aframe -S -C ./certs/localhost.pem -K ./certs/localhost-key.pem"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
